---
# Single source of truth: deploy.sh. It resolves a real PHP CLI binary (the bare `php` on
# this host is CGI and silently drops artisan's arguments) and fails loudly if migrations
# are still pending afterwards, instead of exiting 0 having done nothing.
deployment:
  tasks:
    - bash deploy.sh
