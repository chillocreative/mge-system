// Stub for Task 9 — the full monthly report editor (sections, regenerate,
// finalise/reopen, PDF export) will replace this page.
export default function MonthlyReportEditor() {
    return (
        <div className="flex items-center justify-center py-24 text-sm text-gray-500">
            Loading…
        </div>
    );
}
