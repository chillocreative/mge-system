    body { font-family: 'DejaVu Sans', sans-serif; font-size: 10px; color: #111; }
    table { border-collapse: collapse; }
    .header {
        position: fixed;
        top: -22mm;
        left: 0;
        right: 0;
        height: 18mm;
    }
    .header table { width: 100%; }
    .header td { vertical-align: middle; }
    .header .title-cell { width: 80%; text-align: center; }
    .header .title-cell .project-title { font-size: 10px; font-weight: bold; text-transform: uppercase; }
    .header .title-cell .contract-no { font-size: 9px; text-transform: uppercase; margin-top: 2px; }
    .header .logo-cell { width: 20%; text-align: right; }
    .header .logo-cell img { max-height: 14mm; max-width: 100%; }
    .header hr { border: none; border-bottom: 1px solid #111; margin-top: 3px; }

    .section-title { font-weight: bold; font-size: 12px; margin: 4px 0 8px; }
    .sub-title { font-weight: bold; font-size: 10px; margin: 10px 0 4px; }
    .page-break { page-break-before: always; }
    .avoid { page-break-inside: avoid; }

    table.grid { width: 100%; margin-bottom: 6px; }
    table.grid th, table.grid td { border: 1px solid #111; padding: 3px 5px; font-size: 9.5px; vertical-align: top; text-align: left; }
    table.grid th { background-color: #d9ead3; font-weight: bold; }

    table.kv td { border: 1px solid #111; padding: 4px 6px; font-size: 9.5px; vertical-align: top; }
    table.kv td.label { font-weight: bold; width: 30%; background-color: #f2f2f2; }

    table.kv-plain { width: 100%; margin-bottom: 4px; }
    table.kv-plain td { padding: 3px 4px; font-size: 9.5px; vertical-align: top; }
    table.kv-plain td.kv-label { font-weight: bold; text-transform: uppercase; width: 30%; }
    table.kv-plain td.kv-colon { width: 14px; text-align: center; }
    table.kv-plain td.kv-value { vertical-align: top; }

    .signature-box { border: 1px solid #111; height: 22mm; margin-bottom: 3px; }
    .sig-name { font-weight: bold; }
    .cover-title { text-align: center; font-size: 15px; font-weight: bold; text-transform: uppercase; margin: 6px 0 2px; }
    .cover-period { text-align: center; font-size: 11px; font-weight: bold; margin-bottom: 12px; }
    .cover-logo { text-align: center; margin-bottom: 4px; }
    .cover-logo img { max-height: 26mm; }
    .cover-so-name { text-align: center; font-size: 12px; font-weight: bold; text-transform: uppercase; margin-bottom: 14px; }

    .flow-box { border: 1.5px solid #111; padding: 6px; text-align: center; font-size: 9.5px; }
    .flow-box .t { font-weight: bold; text-transform: uppercase; }
    .flow-arrow { text-align: center; font-size: 14px; padding: 2px 0; }
    table.flow-trunk { width: 60%; margin: 0 auto 4px; }
    table.flow-trunk td { vertical-align: top; padding: 0; }
    table.flow-branch { width: 100%; margin: 4px auto 0; }
    table.flow-branch td.branch-col { width: 45%; vertical-align: top; }
    table.flow-branch td.branch-gap { width: 10%; }
    table.flow-plans { width: 100%; margin-top: 6px; }
    table.flow-plans td { width: 50%; padding: 0 3px; vertical-align: top; }

    .center { text-align: center; }
    .right { text-align: right; }
    .muted { color: #555; font-style: italic; }
    .bmp-check { text-align: center; font-weight: bold; }

    .photo-grid { width: 100%; }
    .photo-grid td { width: 50%; text-align: center; padding: 4px; vertical-align: top; }
    .photo-grid img { max-width: 100%; max-height: 65mm; }
    .photo-caption { font-size: 9px; margin-top: 2px; }
